{
  "name": "@snek-functions/authentication",
  "version": "0.0.1",
  "main": "dist/index.js",
  "type": "module",
  "description": "Generated with `snek-functions init`",
  "author": "snek-at",
  "dependencies": {
    "@snek-at/db-duckdb": "file:packages/snek-db-duckdb",
    "@snek-at/functions": "*"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "serverless": "^3.19.0",
    "serverless-offline": "^9.2.6",
    "serverless-plugin-ifelse": "^1.0.7"
  },
  "scripts": {
    "start": "sf build -f . && IS_OFFLINE=true nodemon --watch dist --exec sls offline --httpPort 4000 --lambdaPort 4002",
    "deploy": "sls deploy",
    "postinstall": "sf install"
  },
  "sfDependencies": {}
}
